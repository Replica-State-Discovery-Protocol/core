/**
 * `@rsdp/core` — the leaderless, modular distributed coordination protocol engine.
 *
 * This is a placeholder entry point so the toolchain has something to build,
 * type-check and test. The real engine will be migrated from the RSDP
 * workbench prototype (`workbench/src/core`).
 */

export const version = '0.0.0';

/** Temporary smoke export proving the package is wired up. */
export function rsdpCoreReady(): boolean {
    return true;
}
